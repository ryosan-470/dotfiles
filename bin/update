#!/bin/bash

OS=`uname`

case $OS in
    "Darwin")
	brew -v update && brew -v upgrade;;
    "Linux")
	sudo apt-get update
	sudo apt-get upgrade;;
    *)
	echo "You use UNKNOWN Operating system. Exit"
	exit 1;;
esac
